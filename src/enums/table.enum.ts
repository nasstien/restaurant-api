export enum TableStatus {
    reserved = 'reserved',
    occupied = 'occupied',
    available = 'available',
}

export enum TableLocation {
    indoor = 'indoor',
    outdoor = 'outdoor',
    privateRoom = 'private room',
}
