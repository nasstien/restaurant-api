export enum OrderStatus {
    received = 'received',
    confirmed = 'confirmed',
    cooking = 'cooking',
    readyForPickup = 'ready for pickup',
    outForDelivery = 'out for delivery',
    delivered = 'delivered',
    cancelled = 'cancelled',
    delayed = 'delayed',
}
